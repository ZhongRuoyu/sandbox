name: Build and merge multi-arch Docker images

on:
  workflow_call:
    inputs:
      dockerhub-username:
        type: string
      ghcr-username:
        type: string
      repository:
        type: string
      primary-tag:
        type: string
      tags:
        type: string
      file:
        type: string
      build-args:
        type: string
      arches:
        type: string
      push:
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ inputs.repository }}-${{ inputs.primary-tag }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash -euxo pipefail {0}

env:
  DOCKERHUB_REPOSITORY: ${{ inputs.dockerhub-username }}/${{ inputs.repository }}
  GHCR_REPOSITORY: ghcr.io/${{ inputs.ghcr-username }}/${{ inputs.repository }}

jobs:
  setup:
    name: Set up ${{ inputs.repository }} build - ${{ inputs.primary-tag }}
    runs-on: ubuntu-latest
    outputs:
      tags: ${{ steps.metadata.outputs.tags }}
      labels: ${{ steps.metadata.outputs.labels }}
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - name: Determine Docker image metadata
        id: metadata
        uses: docker/metadata-action@80c7e94dd9b9319bd5eb7a0e0fe9291e23a2a2e9 # v6.1.0
        with:
          images: |
            ${{ env.DOCKERHUB_REPOSITORY }}
            ${{ env.GHCR_REPOSITORY }}
          flavor: |
            latest=false
          tags: ${{ inputs.tags }}
      - name: Set up build matrix
        id: matrix
        run: |
          matrix_json='{"arch": []}'
          for ARCH in $ARCHES; do
            matrix_json="$(
              echo "$matrix_json" |
                jq --compact-output --arg arch "$ARCH" '.arch += [$arch]'
            )"
          done
          echo "matrix=$matrix_json" >>"$GITHUB_OUTPUT"
        env:
          ARCHES: ${{ inputs.arches }}

  build:
    needs: setup
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.setup.outputs.matrix) }}
    name: Build ${{ inputs.repository }} - ${{ inputs.primary-tag }} (${{ matrix.arch }})
    runs-on: ${{ matrix.arch == 'aarch64' && 'ubuntu-24.04-arm' || 'ubuntu-latest' }}
    permissions:
      contents: read
      packages: write
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0
      - name: Login to Docker Hub
        if: inputs.push
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          username: ${{ inputs.dockerhub-username }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Login to GitHub Container Registry
        if: inputs.push
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ghcr.io
          username: ${{ inputs.ghcr-username }}
          password: ${{ github.token }}
      - name: Set up build
        id: setup
        run: |
          {
            if [[ "$PUSH" = true ]]; then
              echo "outputs<<OUTPUTS"
              echo "type=image,name=$DOCKERHUB_REPOSITORY,name-canonical=true,push=true,push-by-digest=true"
              echo "type=image,name=$GHCR_REPOSITORY,name-canonical=true,push=true,push-by-digest=true"
              echo "OUTPUTS"
            else
              echo "outputs="
            fi
          } >> "$GITHUB_OUTPUT"
        env:
          PUSH: ${{ inputs.push }}
      - name: Build and push Docker image by digest
        id: build
        uses: docker/build-push-action@f9f3042f7e2789586610d6e8b85c8f03e5195baf # v7.2.0
        with:
          file: ${{ inputs.file }}
          platforms: linux/${{ matrix.arch }}
          labels: ${{ needs.setup.outputs.labels }}
          build-args: ${{ inputs.build-args }}
          outputs: ${{ steps.setup.outputs.outputs }}
      - name: Export digest
        run: |
          mkdir -p "$RUNNER_TEMP"/digests
          echo "${DIGEST#sha256:}" >"$RUNNER_TEMP"/digests/"$REPOSITORY-$PRIMARY_TAG-$ARCH"
        env:
          DIGEST: ${{ steps.build.outputs.digest }}
          REPOSITORY: ${{ inputs.repository }}
          PRIMARY_TAG: ${{ inputs.primary-tag }}
          ARCH: ${{ matrix.arch }}
      - name: Upload digest
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: digests-${{ inputs.repository }}-${{ inputs.primary-tag }}-${{ matrix.arch }}
          path: ${{ runner.temp }}/digests/*

  merge:
    if: inputs.push
    needs: [setup, build]
    runs-on: ubuntu-latest
    name: Merge ${{ inputs.repository }} - ${{ inputs.primary-tag }}
    permissions:
      contents: read
      packages: write
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0
      - name: Login to Docker Hub
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          username: ${{ inputs.dockerhub-username }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Login to GitHub Container Registry
        uses: docker/login-action@650006c6eb7dba73a995cc03b0b2d7f5ca915bee # v4.2.0
        with:
          registry: ghcr.io
          username: ${{ inputs.ghcr-username }}
          password: ${{ github.token }}
      - name: Download digests
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          path: ${{ runner.temp }}/digests
          pattern: digests-${{ inputs.repository }}-${{ inputs.primary-tag }}-*
          merge-multiple: true
      - name: Create manifest list and push
        run: |
          mapfile -t tags <<< "$TAGS"
          tag_args=()
          for tag in "${tags[@]}"; do
            tag_args+=(--tag="$tag")
          done
          images=()
          for ARCH in $ARCHES; do
            images+=("ghcr.io/$GHCR_USERNAME/$REPOSITORY@sha256:$(cat "$RUNNER_TEMP"/digests/"$REPOSITORY-$PRIMARY_TAG-$ARCH")")
          done
          docker buildx imagetools create "${tag_args[@]}" "${images[@]}"
        env:
          DOCKERHUB_USERNAME: ${{ inputs.dockerhub-username }}
          GHCR_USERNAME: ${{ inputs.ghcr-username }}
          REPOSITORY: ${{ inputs.repository }}
          PRIMARY_TAG: ${{ inputs.primary-tag }}
          ARCHES: ${{ inputs.arches }}
          TAGS: ${{ needs.setup.outputs.tags }}
