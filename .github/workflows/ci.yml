name: ci

on:
  push:
    branches:
      - main

jobs:
  docker:
    strategy:
      matrix:
        target:
          - tags: zhongruoyu/sandbox:latest,zhongruoyu/sandbox:ubuntu,zhongruoyu/sandbox:jammy
            file: jammy.Dockerfile
            platforms: linux/amd64,linux/arm64
          - tags: zhongruoyu/sandbox:focal
            file: focal.Dockerfile
            platforms: linux/amd64,linux/arm64
          - tags: zhongruoyu/sandbox:fedora
            file: fedora.Dockerfile
            platforms: linux/amd64,linux/arm64
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Build and push Docker images
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ${{ matrix.target.file }}
          platforms: ${{ matrix.target.platforms }}
          push: true
          tags: ${{ matrix.target.tags }}
